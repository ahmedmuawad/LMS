<?php

declare(strict_types=1);

/*
 | حالة بناء كل ميزة معروضة على صفحة الباقات.
 |
 | المرجع: docs/reports/features-audit.md — فحصٌ للكود لا للوثائق.
 |
 | الميزة المدرجة هنا تُعرض موسومةً «قريباً»: تبقى في الباقة فلا
 | يُخفى ما وعدنا به، ولا يشتريها أحد ظانّاً أنها جاهزة اليوم. ومتى
 | بُنيت تُحذف من هذه القائمة فيسقط الوسم من كل مكان دفعةً واحدة.
 |
 | القائمة مصدرٌ واحد: صفحة الباقات، ولوحة المشترك، وشاشة الترقية،
 | كلّها تقرأ منها — فلا تفترق واجهةٌ عن أخرى.
 */

return [

    'coming_soon' => [

        /*
         | حماية المحتوى.
         |
         | `screenshot_block` بُني بما يمكن في متصفّح: طمسٌ عند فقد
         | التركيز ومنعُ نسخٍ وطباعة — ولا يُوعَد بأكثر.
         |
         | و`offline_download` مبنيّ للملفّ المرفوع: يُحفظ داخل
         | المنصة في جهاز الطالب فيُشاهَد بلا إنترنت ولا يخرج ملفّاً
         | يُنسَخ. ولا يعمل لفيديو YouTube أو Vimeo — تلك تُقدَّم من
         | خوادمها بمشغّلاتها ولا نملك بايتاتها.
         |
         | أمّا `drm` (تشفير الفيديو برخصةٍ لكل مشاهدة) فيحتاج مزوّد
         | فيديو يدعم Widevine/FairPlay وحسابَ DRM عنده — ولا يُبنى
         | من طرفنا وحده.
         */
        'drm',

        /*
         | Jitsi يعمل بلا مفاتيح؛ وZoom وBigBlueButton مبنيّان
         | ويحتاجان بيانات ربط (لذا في `needs_setup` أدناه). أمّا
         | Google Meet فيحتاج موافقة OAuth لكل مدرّس، ولم يُبنَ بعد.
         */
        'live_meet',

        /*
         | الهوية البيضاء متحقّقة فعلاً: لا اسم لنا في أي صفحة
         | يراها الطالب — الترويسة والتذييل والبريد وصفحة الدخول
         | كلّها باسم المشترك، وله نطاقه الخاص.
         */

        /*
         | `coaching` مبنيّ: هو نوع الخدمة «موعد محجوز» — تقويمُ
         | المقدّم وأوقاته وحجزُ ساعةٍ منه لا تُباع مرّتين. وهو
         | محجوزٌ على من يحمل المفتاح وحده.
         */

        // الموجود تطبيق ويب تقدّمي (PWA)، لا تطبيق متجر
        'mobile_app',
    ],

    /*
     | مبنيّ ويحتاج مفاتيح مزوّد قبل أن يعمل.
     |
     | ليس «قريباً» — فالكود جاهز — لكن عرضه بلا تنبيه يجعل المشترك
     | يظنّه يعمل من أول يوم.
     */
    'needs_setup' => [
        'sms',
        'whatsapp',
        'live_zoom',
        'live_bbb',

        /*
         | `whatsapp_api` هو قناة WhatsApp Cloud API المبنيّة —
         | «واتساب للأعمال» في القاعدة. تحتاج `phone_id` و`token`
         | من حساب المشترك على Meta، ولا تحتاج كوداً.
         */
        'whatsapp_api',
    ],
];
