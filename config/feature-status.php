<?php

declare(strict_types=1);

/*
 | حالة بناء كل ميزة معروضة على صفحة الباقات.
 |
 | المرجع: docs/reports/features-audit.md — فحصٌ للكود لا للوثائق.
 |
 | الميزة المدرجة هنا تُعرض موسومةً «قريباً»: تبقى في الباقة فلا
 | يُخفى ما وعدنا به، ولا يشتريها أحد ظانّاً أنها جاهزة اليوم. ومتى
 | بُنيت تُحذف من هذه القائمة فيسقط الوسم من كل مكان دفعةً واحدة.
 |
 | القائمة مصدرٌ واحد: صفحة الباقات، ولوحة المشترك، وشاشة الترقية،
 | كلّها تقرأ منها — فلا تفترق واجهةٌ عن أخرى.
 */

return [

    'coming_soon' => [

        // تعلّم متقدّم — لا مشغّل ولا تتبّع
        'h5p',
        'xapi',

        /*
         | حماية المحتوى.
         |
         | `screenshot_block` بُني بما يمكن في متصفّح: طمسٌ عند فقد
         | التركيز ومنعُ نسخٍ وطباعة — ولا يُوعَد بأكثر.
         | و`drm` و`offline_download` يحتاجان مزوّد فيديو مشفَّراً.
         */
        'drm',
        'offline_download',

        /*
         | Jitsi مبنيّ: غرفةٌ تُنشأ لكل حصة باسمٍ لا يُخمَّن، ونافذةُ
         | دخولٍ حول الموعد. أمّا Zoom وMeet وBBB فتحتاج ربط حساب،
         | ولم تُبنَ بعد.
         */
        'live_zoom',
        'live_meet',
        'live_bbb',

        // الذكاء الاصطناعي والتكامل
        'ai_assistant',
        'ai_course_builder',
        'whatsapp_api',

        /*
         | الهوية البيضاء متحقّقة فعلاً: لا اسم لنا في أي صفحة
         | يراها الطالب — الترويسة والتذييل والبريد وصفحة الدخول
         | كلّها باسم المشترك، وله نطاقه الخاص.
         */

        // الموجود تطبيق ويب تقدّمي (PWA)، لا تطبيق متجر
        'mobile_app',
        'coaching',
    ],

    /*
     | مبنيّ ويحتاج مفاتيح مزوّد قبل أن يعمل.
     |
     | ليس «قريباً» — فالكود جاهز — لكن عرضه بلا تنبيه يجعل المشترك
     | يظنّه يعمل من أول يوم.
     */
    'needs_setup' => [
        'sms',
        'whatsapp',
    ],
];
